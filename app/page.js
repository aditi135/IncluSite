"use client";

import Head from "next/head";
// import "../app/styles/main.css"
import styles from "./styles/main.module.css";
import Filters from "./components/Filters";
// import FilterBox from "./components/FilterBox";
import SearchBar from "./components/SearchBar";
import { useState } from "react";
import SearchResultsList from "./components/SearchResultsList";

export default function Page() {
  const [results, setResults] = useState([]);

  return (
    <>
      {/* <Head>
          <title>IncluSITE</title>
          <meta name="description" content="Generated by create next app" />
          <meta name="viewport" content="width=device-width, initial-scale=1" />
          <link rel="icon" href="/favicon.ico" />
        </Head> */}

      {/* <main className={`${styles.container}`}>
          <div className={`${styles.windowImage}`}>

            <h1>IncluSITE</h1>

          </div>

          <div className="search-bar-container">
            <SearchBar setResults={setResults}></SearchBar>
            {results && results.length > 0 && <SearchResultsList results={results} />}
          </div>
          <div>
            <h2>Filter for Accessibility Features</h2>

            // <FilterBox text="Arias, Image Descriptions, and Screen-reader Support"></FilterBox> 
            <input type="checkbox" value="visual"></input>
            <label htmlFor="aria">Arias, Image Descriptions, and Screen-reader Support</label>

            <input type="checkbox" value="audio"></input>
            <label htmlFor="audio">Video Captioning</label>

            <input type="checkbox" value="lowvision"></input>
            <label htmlFor="lowvision">Text and Color Contrast</label>
          </div>

        </main> */}

      <Head>
        <title>IncluSITE</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={styles.container}>
        <h1>IncluSITE</h1>
        <p>
          IncluSITE is a project by a group of students at the University of
          Illinois at Urbana-Champaign that seeks to increase web accessibility.
          We have compiled a database of commonly used federal websites and used
          the Axe-Core API to rank them by how many accessiblity features they
          contain. You can search for federal services like "usps,"
          "healthcare," "irs," and more to find a website that works for you.
        </p>
        <p>
          Our filtering system is currently offline as we improve our tagging
          system. If you have any feedback on our project, please contact us at:
        </p>
        <p>
          <a href="mailto:aditiak4@illinois.edu?cc=jiasg2@illinois.edu&subject=IncluSITE%20Project%20Feedback">
            aditiak4@illinois.edu
          </a>
          <br />
          <a href="mailto:jiasg2@illinois.edu?cc=aditiak4@illinois.edu&subject=IncluSITE%20Project%20Feedback">
            jiasg2@illinois.edu
          </a>
        </p>

        <div className="search-bar-container">
          <SearchBar setResults={setResults}></SearchBar>
          {/* {results && results.length > 0 && <SearchResultsList results={results} />} */}
        </div>

        <div className={styles.columns}>
          <div className={styles.left}>
            <Filters></Filters>
          </div>

          <div className={styles.right}>
            <h2>Results</h2>
            {results && results.length > 0 && (
              <SearchResultsList results={results} />
            )}
          </div>
        </div>
      </main>
    </>
  );
}
